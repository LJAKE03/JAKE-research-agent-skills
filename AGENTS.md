# Research Skills Operating Rules

以下非简单科研任务优先读取并遵循 `00-research-orchestrator/SKILL.md`：

- 需要跨来源证据综合、多个相互依赖的步骤或文件；
- 涉及论文、综述、报告、方法、技术路线或高影响科研判断；
- 目标或成功标准不清，且缺失信息会实质改变路线或交付物。

单一事实查证、局部改写、已定位文件的小修和其他低风险短任务直接完成，不启动完整总控流程。

本仓库只有一条科研流程。用户可在项目级选择战略总控、证据 Worker 和输出 Worker 的模型与 reasoning；总控决定是否调用功能 Skill、是否委派、是否分阶段和使用何种质量检查。

执行规则：

1. 战略总控是非简单科研任务的入口、阶段返回点和最终用户接口。
2. `research_support` 只接收有界证据任务卡，执行检索、网页查证、文件扫描、提取和证据表整理。
3. `research_output` 只接收完整锁定输出包，生成低判断的文本、表格、语言和格式规格，不新增事实、引用、公式、计算、分类、优先级或科研判断，也不直接编辑文件。
4. 研究方向、任务拆解、方法、参数、边界、来源可靠性、证据综合和最终结论由战略总控负责。
5. 只有真实依赖才创建阶段；只有任务可独立分离且收益超过调用与交接开销时才委派。简单、低风险任务直接完成。
6. Worker 不得互相转交或递归委派，统一按紧凑交接 Schema 返回战略总控。
7. 模型间不复制完整对话、全部项目历史、全部工具日志或整篇论文原文。
8. 投稿、申报、安全或高成本、关键参数、核心方法和最终科学结论由战略总控做一次紧凑语义验收。
9. 只在阶段切换、跨轮恢复或高影响变化时更新项目状态。
10. 按 `shared/PROACTIVE_INQUIRY_AND_MEMORY_PROTOCOL.md` 先查后问，只询问用户独有且会改变路线或交付物的问题。
11. 本地科研代码的跨文件结构、数据流、复现路径或改动影响可按需调用 `07-code-context`；CodeGraph 不自动安装或初始化，缺失时回退 `rg` 和定点读取。
12. 项目经验默认留在项目复盘中。只有用户明确批准的跨项目经验才能进入个人全局记忆。
13. 已授权且可逆的 Skill 修改直接完成并运行相关检查；发布、不可逆删除或超出既有授权的高影响变更再请求确认。

修改 Skill、代码、脚本、Schema、配置、模板、说明或测试且影响多个活动文件时，读取 `shared/CHANGE_INTEGRITY_PROTOCOL.md`。保留原始数据、失败记录、决策依据、复盘和版本历史。

Windows PowerShell 读取仓库文本时显式使用 `Get-Content -Encoding UTF8`。优先用 `rg`、限定行或摘要读取大型文件；包含非 ASCII 的 Windows PowerShell 5.1 脚本保留 UTF-8 BOM。

`shared/MODEL_ROUTING.json` 保存默认模型、安全边界和委派契约；项目级 `.research-agent/MODEL_ROUTING.selection.json` 只保存用户选择。默认是战略总控 `gpt-5.6-sol/xhigh`、证据 Worker `gpt-5.6-terra/medium`、输出 Worker `gpt-5.6-luna/low`。战略总控可选 Astra 或 Sol；两个 Worker 可选 Sol、Terra 或 Luna。模型选择在新任务启动时生效，不会切换已经运行的主线程。
